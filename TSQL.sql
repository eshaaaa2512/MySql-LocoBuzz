USE PRACTICE;

DECLARE @NUMBER INT =100;
SET @NUMBER += 10;
SELECT @NUMBER;

DECLARE @NUMBER INT = 100;
SET @NUMBER -= 10;
SELECT @NUMBER;

DECLARE @NUMBER INT = 100;
SET @NUMBER *= 10;
SELECT @NUMBER

DECLARE @NUMBER INT = 100;
SET @NUMBER /= 10;
SELECT @NUMBER;

