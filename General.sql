SELECT EMP_NAME,EMP_SALARY FROM EMPLOYEE --- SUB QUERY
WHERE EMP_SALARY>(SELECT EMP_SALARY FROM EMPLOYEE WHERE EMP_NAME ='ADAM')
AND EMP_DEPTID = (SELECT EMP_DEPTID FROM EMPLOYEE WHERE EMP_NAME = 'ADAM');

