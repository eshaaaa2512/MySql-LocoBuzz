
--- FOR AVGERAGE SALARY
WITH CTE_AVGSALARY AS
( 
     SELECT AVG(EMP_SALARY) AS AVGSALARY FROM EMPLOYEE
)

SELECT AVGSALARY FROM CTE_AVGSALARY;

---FOR COUNT
WITH CTE_COUNTSALARY AS
(
        SELECT COUNT(EMP_SALARY) AS COUNTSALARY FROM EMPLOYEE
)
SELECT COUNTSALARY FROM CTE_COUNTSALARY;